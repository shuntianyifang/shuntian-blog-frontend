# 新博客前端开发约定

## 定位与边界

- 本仓库使用 Nuxt、Vue、TypeScript；公开页面 SSG，管理后台 CSR。
- Go API 与 PostgreSQL 位于独立后端仓库，本仓库不直连数据库。
- 不引入生产 Node SSR，不用 Nuxt server routes 替代 Go 业务 API。
- 不全局禁用 SSR；产物须能由 Nginx 静态托管。
- 不依赖父目录文档或相邻仓库才能安装、构建和运行自身测试。

## 数据与页面

- 构建从 Go 获取固定公开内容快照，不拼接文章仓库数据。
- 显式生成文章、分类、标签和分页；必要路由生成失败则构建失败。
- /admin 深层路由刷新使用管理入口，公开页面不存在时返回真实 404。
- 草稿预览走鉴权 API，不进入 HTML、payload 或搜索索引。
- 浏览器使用同域 /api/；凭据不可进入客户端配置或静态产物。
- 路由守卫不是权限防线；界面区分已保存、待发布和已上线。
- 组件与样式按实际复用提取，不提前建立复杂抽象。

## 接口、验证与部署

- 接口契约以后端仓库文档为准，跨仓库变更关联对应改动。
- GitHub Actions 构建并部署，旧任务不可覆盖新版本，失败保留旧站。
- 保留旧页面及回滚需要的哈希资源。
- 使用 package.json、pnpm-lock.yaml 锁定的 Node 22.20.0、pnpm 10.18.3 与依赖版本；命令以 README 为准。
- 验证完整 HTML、后台直接访问、404、静态资源及敏感数据未泄漏。
- 不运行旧 Astro 项目命令代替本仓库验证。

## 第一轮实际实现与检查

- 已实现 Nuxt 4.5.2 SSG、Vue CSR 后台和固定快照 v1；没有自动发布、生产部署、媒体上传或搜索。
- `pnpm install --frozen-lockfile`、`pnpm typecheck`、`pnpm test`、`pnpm generate:fixture` 可独立运行。真实构建用 `pnpm generate`，必须明确提供快照文件或构建 API 配置，禁止隐式退回夹具。
- 浏览器集成验收用 `scripts/acceptance.ps1`，明确传入后端环境文件与演示账号文件；它会向本地开发库追加测试数据、重建本地静态产物，不能指向生产环境。
- `.local/`、`.generated/`、`.output/` 不提交；API token、管理员密码仅本地/服务端读取，不能进入公开配置。截图中的草稿标记为人工测试数据。
- Nginx 本地脚本只管理本项目独立实例；本地构建不提供生产原子切换保证。

## 协作与版权

- 默认用中文说明改动和实际验证结果，区分计划与已实现功能。
- 尊重已有修改，不进行无关重构、目录移动或 Git 初始化。
- 本地开发不自动授权生产部署、服务重启或数据删除；已有明确授权不重复询问。
- 作者原创内容暂不授权他人复用，不擅自添加开源许可证；保留第三方许可、署名和引用。
- 命令与版本以本仓库实际文件为准，未执行的检查不得报告通过。
